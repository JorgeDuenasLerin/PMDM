<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.duenaslerin.pmdm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".U410EjemploToast">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
        <activity android:name=".U4LayoutControles"></activity>
        <activity android:name=".U4LayoutsEventos" />
        <activity android:name=".U410EjemploToast"></activity>
        <activity android:name=".U49Pruebas"></activity>
        <activity android:name=".SegundaActividad" />
        <activity android:name=".IntegraccionGit" />
        -->
    </application>

</manifest>